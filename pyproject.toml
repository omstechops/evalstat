[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "evalstat"
version = "0.0.1"
description = "Statistical validity tooling for LLM and agent evaluations."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "omstechops", email = "325673804+omstechops@users.noreply.github.com" },
]
keywords = ["evaluation", "statistics", "llm", "power-analysis", "bootstrap"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
]

[project.urls]
Homepage = "https://github.com/omstechops/evalstat"
Issues = "https://github.com/omstechops/evalstat/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/evalstat"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q --strict-markers --strict-config"
markers = [
    "slow: coverage simulations; hundreds of bootstraps, minutes not seconds",
]
filterwarnings = ["error"]

[tool.coverage.run]
source = ["evalstat"]
branch = true

[tool.ruff]
line-length = 88
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # bugbear
    "UP",   # pyupgrade
    "N",    # pep8-naming
    "D",    # pydocstyle
    "ANN",  # missing type annotations
    "NPY",  # numpy-specific rules
    "RUF",
]
ignore = [
    "D107",  # __init__ docstring lives on the class
    "ANN401",
]

[tool.ruff.lint.per-file-ignores]
# Tests describe themselves through their names; no docstring/annotation nagging.
"tests/**" = ["D100", "D103", "ANN001", "ANN201"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
# numpy ships stubs that use `type` statements, which mypy only parses when its
# target is 3.12+. requires-python was raised to match: a >=3.10 floor that
# neither mypy nor any test run actually exercises is an unverified claim, and
# unverified claims do not get published. A 3.10 floor can come back when CI
# runs a 3.10 matrix that proves it.
python_version = "3.12"
files = ["src", "tests"]
strict = true

[[tool.mypy.overrides]]
# scipy publishes no stubs of its own; only norm.ppf/norm.cdf are used here.
module = "scipy.*"
ignore_missing_imports = true
